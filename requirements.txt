# Python deps for the deepeval/ and ragas/ scripts.
# Promptfoo is a Node tool, run via `npx promptfoo@latest` (no pip needed).
#
# NOTE: deepeval and ragas have a known `click` version conflict when installed
# in the SAME venv (deepeval wants click<8.4.0, langchain-anthropic pulls >=8.4.2).
# Cleanest fix is a separate virtualenv per framework. See each RUN.md.

deepeval>=4.1.5
ragas>=0.4.3
langchain-anthropic>=1.5.4
anthropic>=0.121.0
